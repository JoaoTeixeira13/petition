{{>navbar loggedIn=true signedIn=true}}

<section class="profile-edit">

    <h2> Edit your profile </h2>

        <img src="/assets/workerBee.jpg" alt="worker bee" class="form-img edit">


    <div class="profileEdit-failure">
        {{#if error}}
            <p> Uh, oh. Something went wrong. please try again.</p>
        {{/if}}

    </div>

    <form method="POST" class="profile-form">
        

        <label for="fName" class="profile-label">
            <p>First Name</p>
            <input
                class="input-field"
                type="text"
                name="fName"
                pattern="[A-Za-z]+"
                title="Provide only valid characters."
                value="{{profile.first}}"
                required
                
            />
        </label>

        <label for="lName" class="profile-label">
            <p>Last Name</p>
            <input
                class="input-field"
                type="text"
                name="lName"
                pattern="[A-Za-z]+"
                title="Provide only valid characters."
                value="{{profile.last}}"
                required
            />
        </label>

        <label for="email" class="profile-label">
            <p>Email Address</p>
            <input
                class="input-field"
                type="email"
                name="email"
                value="{{profile.email}}"
                required
            />
        </label>

        <label for="password" class="profile-label">
            <p>Password</p>
            <input class="input-field" type="password" name="password" />
        </label>

        <label for="age" class="profile-label">
            <p>Age</p>
            <input
                class="input-field"
                type="number"
                name="age"
                value="{{profile.age}}"
            />
        </label>

        <label for="city" class="profile-label">
            <p>City</p>
            <input
                class="input-field"
                type="text"
                name="city"
                pattern="[A-Za-z\s]+"
                title="Provide only valid characters."
                {{#if profile.city}}
                value="{{profile.city}}"
                {{/if}}
            />
        </label>
        
        <label for="url" class="profile-label">
            <p>Homepage</p>
            <input
                class="input-field"
                type="url"
                name="url"
                {{#if profile.url}}
                value="{{profile.url}}"
                {{/if}}
            />
        </label>

        <button class="submit-button">Update</button>

    </form>

    <a href="/profile/delete" class="singular-login warning"> Delete account </a>

</section>